<main class="w-full flex flex-col">
  <button
    class="ir px-4 border-2 border-green-700 text-green-500 text-shadow-green-700 font-bold place-self-center disabled:bg-gray-200 disabled:text-shadow-none disabled:border-gray-900 disabled:text-gray-500 disabled:opacity-50 disabled:cursor-not-allowed"
    id="empezarbtn"
  >
    EMPEZAR! 🎲
  </button>
</main>

<style>
  .ir {
    background-image: url("../verde.png");
    background-size: cover;
    background-position: bottom;
    position: relative;
    -webkit-text-stroke: 1px rgba(0, 140, 0, 0.2);
    margin-top: 20px;
    font-size: 30px;
  }

  .ir:hover {
    -webkit-text-stroke: 1px rgb(0, 140, 0);
    cursor: pointer;
  }

  .ir:hover::before {
    opacity: 1;
  }

  .ir::before {
    opacity: 0;
    transition: all 0.3s ease;
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      rgba(5, 219, 16, 0.5) 0%,
      rgba(65, 197, 13, 0) 50%,
      rgba(5, 219, 16, 0.5) 100%
    );
    z-index: 10;
  }

  .ir:disabled::before {
    background: linear-gradient(
      90deg,
      rgba(5, 180, 16, 0.5) 0%,
      rgba(65, 197, 13, 0) 50%,
      rgba(5, 180, 16, 0.5) 100%
    );
  }
</style>
